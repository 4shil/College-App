Department (HoD) module (teacher-side)

Current implementation:
- Shows active HoD assignments (if `hod_assignments` exists and RLS permits)
- Shows a limited teacher list (best-effort; may be blocked by RLS)
