table:
  name: years
  schema: public
array_relationships:
  - name: sections
    using:
      foreign_key_constraint_on:
        column: year_id
        table:
          name: sections
          schema: public
  - name: semesters
    using:
      foreign_key_constraint_on:
        column: year_id
        table:
          name: semesters
          schema: public
  - name: students
    using:
      foreign_key_constraint_on:
        column: year_id
        table:
          name: students
          schema: public
